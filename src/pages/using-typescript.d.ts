import React from "react";
import { PageProps } from "gatsby";
declare type DataProps = {
    site: {
        buildTime: string;
    };
};
declare const UsingTypescript: React.FC<PageProps<DataProps>>;
export default UsingTypescript;
export declare const query: void;
